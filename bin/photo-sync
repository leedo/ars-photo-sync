#!/usr/bin/env perl

use App::Photosync;
use Getopt::Long;

my %opts;

GetOptions(
  "source=s"   => \($opts{source}),
  "dest=s"     => \($opts{dest}),
  "secret=s"   => \($opts{secret}),
  "key=s"      => \($opts{key}),
);

App::Photosync->new(%opts)->run;
