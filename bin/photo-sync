#!/usr/bin/env perl

use App::Photosync;
use Getopt::Long;

my %opts;

GetOptions(
  "source=s"   => \($opts{source}),
  "event=s"     => \($opts{event}),
  "secret=s"   => \($opts{secret}),
  "key=s"      => \($opts{key}),
  "bucket=s"   => \($opts{bucket}),
);

App::Photosync->new(%opts)->run;
